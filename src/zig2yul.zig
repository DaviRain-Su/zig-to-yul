pub fn range_step(start: i256, end: i256, step: i256) []const i256 {
    _ = start;
    _ = end;
    _ = step;
    // IDE helper only; zig-to-yul handles semantics during transformation.
    return &[_]i256{};
}
